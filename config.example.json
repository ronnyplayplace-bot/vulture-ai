{
  "_about": "Vulture AI (Overlkd Studio) configuration. Copy this file to 'config.json' (same folder, next to studio.py) and edit the values for YOUR machine. Nothing here is machine-specific by design: pick your own drive letter, username and install location.",
  "_how_it_works": "The loader (vulture/config.py) layers three sources: built-in defaults  <-  AUTO-DETECTION (scans all drives for ComfyUI / Ollama / Python)  <-  this file. So you only need to set the values auto-detection cannot guess (or the ones it guessed wrong). Any key you delete or leave as \"\" (empty string) falls back to auto-detect, then to a sensible default.",
  "_notes": [
    "Forward slashes '/' and back slashes '\\\\' both work; the loader normalises them for Windows.",
    "Keys starting with '_' or '//' are comments and are ignored by the loader.",
    "Leave a path as \"\" to let auto-detection fill it in.",
    "This is drive-agnostic: if your ComfyUI lives on E: or C:, just change the drive letter."
  ],

  "paths": {
    "_comment": "Absolute locations of the tools. Change the drive letter/username to match your PC.",

    "comfy_dir": "D:/comfyui/ComfyUI",
    "_comfy_dir": "The ComfyUI application folder (the one that contains main.py and the 'models' + 'custom_nodes' subfolders).",

    "comfy_python": "D:/comfyui/venv/Scripts/python.exe",
    "_comfy_python": "python.exe of ComfyUI's virtual environment. For the portable build this is '<portable>/python_embeded/python.exe'. Leave \"\" to auto-detect.",

    "comfy_models_dir": "",
    "_comfy_models_dir": "Where checkpoints/unet/vae/... live. Empty => derived as <comfy_dir>/models.",

    "comfy_input_dir": "",
    "_comfy_input_dir": "ComfyUI input folder (source images are copied here). Empty => derived as <comfy_dir>/input.",

    "output_dir": "D:/comfyui/output",
    "_output_dir": "Where generated images/videos are written and opened from.",

    "tools_dir": "",
    "_tools_dir": "Optional: the 'ai-memory-tools' folder holding start-webui.cmd / start-local-memory.cmd (Chat + Code-RAG). Leave \"\" if you do not use these extras.",

    "launchers_dir": "",
    "_launchers_dir": "Folder that holds Overlkd-Start.cmd, Overlkd-Coder.cmd, Overlkd-Status.cmd. Empty => the repo root (folder that contains studio.py).",

    "ollama_exe": "",
    "_ollama_exe": "Path to ollama.exe. Empty => auto-detect (%LOCALAPPDATA%/Programs/Ollama/ollama.exe or PATH).",

    "ollama_models_dir": "D:/ollama/models",
    "_ollama_models_dir": "Where Ollama stores models (sets the OLLAMA_MODELS env var). Empty => Ollama default (~/.ollama/models).",

    "system_python": "",
    "_system_python": "System Python 3.11 (used by Overlkd-Coder.cmd helpers). Empty => auto-detect.",

    "aider_python": "D:/ai-coder/venv/Scripts/python.exe",
    "_aider_python": "python.exe of the Aider coding-agent venv. Optional (coding agent only).",

    "hf_cache_dir": "D:/hf-cache",
    "pip_cache_dir": "D:/pip-cache",
    "_caches": "Optional: keep HuggingFace + pip caches off C:. Empty => system defaults.",

    "qdrant_path": "",
    "_qdrant_path": "Code-RAG: local embedded-Qdrant store (your code never leaves the PC; this is NOT a server). Empty => %LOCALAPPDATA%/VultureAI/rag/qdrant. Point it at a roomy drive if you index a lot.",

    "rag_data_dir": "",
    "_rag_data_dir": "Code-RAG: folder for the project registry; the fastembed model cache is kept next to it. Empty => %LOCALAPPDATA%/VultureAI/rag/data."
  },

  "network": {
    "_comment": "Host + ports. Only change if a port clashes with something else.",
    "host": "127.0.0.1",
    "comfy_port": 8188,
    "webui_port": 8080,
    "ollama_port": 11434,
    "rag_port": 8001
  },

  "runtime": {
    "_comment": "Behaviour / performance knobs.",

    "cuda_device": 0,
    "_cuda_device": "GPU index passed to ComfyUI (--cuda-device).",

    "vram_tier": "auto",
    "_vram_tier": "auto | s | m | l | xl. 'auto' asks nvidia-smi. s=6GB, m=12GB, l=16GB, xl=24GB+. Controls the ComfyUI VRAM flag and the coding-agent context size.",

    "comfy_vram_flag": "",
    "_comfy_vram_flag": "Override the ComfyUI memory flag. Empty => chosen from vram_tier ('--lowvram' for s, '--fast' for m/l/xl). Set e.g. '--normalvram' or '--novram' to force.",

    "comfy_extra_args": "",
    "_comfy_extra_args": "Extra CLI args appended to the ComfyUI start command (e.g. '--use-pytorch-cross-attention').",

    "enhance_model": "qwen2.5-coder:7b",
    "_enhance_model": "Ollama model used by the 'optimise prompt' feature (turns casual/German text into an English FLUX prompt).",

    "coder_model": "qwen2.5-coder:7b",
    "_coder_model": "Default Ollama model for the coding agent. The author prefers 'qwen3.5:9b' on bigger GPUs; 'qwen2.5-coder:7b' is the safe 6GB default.",

    "embed_model": "BAAI/bge-small-en-v1.5",
    "_embed_model": "Code-RAG: fastembed embedding model (CPU/ONNX, downloaded on first run). bge-small is a fast, small default; change only if you understand the size/quality trade-off.",

    "rag_collection": "vulture_code",
    "_rag_collection": "Code-RAG: the Qdrant collection to index into. Use different names to keep separate indexes side by side."
  },

  "models": {
    "_comment": "Filenames used inside the ComfyUI workflows. These must match what the installer put in <comfy_models_dir>. Change only if you downloaded a different quant/variant.",
    "flux_unet": "flux1-schnell-Q4_K_S.gguf",
    "flux_t5_gguf": "t5-v1_1-xxl-encoder-Q8_0.gguf",
    "flux_t5_fp8": "t5xxl_fp8_e4m3fn.safetensors",
    "flux_clip_l": "clip_l.safetensors",
    "flux_vae": "flux_ae.safetensors",
    "upscale_model": "4x-UltraSharp.pth",
    "swap_model": "inswapper_128.onnx",
    "restore_model": "codeformer.pth"
  }
}
